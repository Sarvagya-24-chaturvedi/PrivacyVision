services:
  - type: web
    name: privacyvision-backend
    env: python
    buildCommand: "pip install -r server/requirements.txt"
    startCommand: "uvicorn server.app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
